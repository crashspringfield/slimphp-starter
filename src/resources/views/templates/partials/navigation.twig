<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Authentication App</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path_for('home') }}">
          Home<span class="sr-only">(current)</span>
        </a>
      </li>
      {% if auth.check %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('takeQuiz') }}">
            Take quiz
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('reviewQuiz') }}">
            Review quiz
          </a>
        </li>
      {% endif %}
    </ul>
    <ul class="navbar-nav">
      {% if auth.check %}
        <li class="nav-item">
          {{ auth.user.email }}
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('auth.signout') }}">Sign Out</a>
        </li>
      {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('auth.signup') }}">Sign Up</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path_for('auth.signin') }}">Sign In</a>
        </li>
      {% endif %}
    </ul>
  </div>
</nav>
